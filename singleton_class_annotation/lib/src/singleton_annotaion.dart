/// A simple `@SingletonClass()` annotation
/// Currently, This annotation supports a no-args singleton only.
class SingletonClass {
  /// To use this class as an annotation, we need a const constructor.
  const SingletonClass();
}
